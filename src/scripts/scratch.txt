document.addEventListener("DOMContentLoaded", () => {
   let nextLinkTarget = document.querySelector("link[rel=next]").getAttribute("href")
   document.querySelectorAll('a.button-link.next').forEach((elm) => {
     elm.setAttribute('href', nextLinkTarget);
   });
  // console.log(gitbook.getState());
  // console.log(gitbook.hasChanged);
  // let nextLinkTarget = document.querySelector("link[rel=next]").getAttribute("href");
  // console.log(document.readyState)
  // console.log(nextLinkTarget);
  // console.log(window.popstate);
  // document.querySelectorAll('a.button-link.next')[0].setAttribute("href", nextLinkTarget);
  // console.log(document.querySelectorAll('a.button-link.next')[0].href);
});



<div class="buttons">
  <a href="#" class="button-link prev" onclick="gitbook.navigation.goPrev()">Back</a>
  <a href="#" class="button-link next" onclick="gitbook.navigation.goNext()">I have built my application locally</a>
</div>
