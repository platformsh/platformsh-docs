web:
    upstream:
        socket_family: unix
    commands:
        start: "unicorn -l $SOCKET -E production config.ru"
    locations:
        /:
            root: "public"
            passthru: true
            expires: 1h
            allow: true