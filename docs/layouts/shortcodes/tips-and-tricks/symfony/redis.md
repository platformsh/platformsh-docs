Connect to remote Redis using Symfony CLI
  ```bash
  symfony cloud:redis
  ```

Connect to DDEV Redis service
  ```bash
  ddev redis-cli
  ```

Connect to Local Redis using [redis-cli](https://redis.io/docs/getting-started/)
  ```bash
  redi-cli
  ```

