List all existing SSH tunnels
  ```bash
  symfony tunnels
  ```

Open SSH tunnels to current environment
  ```bash
  symfony tunnel:open
  ```

Get tunnel info
  ```bash
  symfony tunnel:info
  ```

Close SSH tunnels
  ```bash
  symfony tunnel:close --all -y
  ```

