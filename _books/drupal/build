if [[ $# -eq 0 ]] ; then
    echo 'You need to specifiy a book to build'
    echo 'ex: build drupal'
    echo 'Available books\n'
    ls book_*.json | sed -e 's/^book_(.*)\.json$/ccc/g'
    exit 0
fi
export BOOK=$1 
rm -rf _books
gitbook build
mkdir -p _books
mv _book _books/$1